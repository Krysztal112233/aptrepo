set no-cd

build suite="trixie":
    @just setup-rust
    ./packages/mdbook/build.ts "{{ suite }}"

build-all:
    @just mdbook::build bookworm
    @just mdbook::build trixie
    @just mdbook::build forky
