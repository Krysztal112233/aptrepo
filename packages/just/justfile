set no-cd

build suite="trixie" arch="all":
    ./scripts/build-targets.ts --build {{quote(suite)}} {{quote(arch)}}
    @just setup-rust {{quote(arch)}}
    ./packages/just/build.ts {{quote(suite)}} {{quote(arch)}}

build-all arch="all":
    ./scripts/build-targets.ts --build-all {{quote(arch)}}
    @just just::build bookworm {{quote(arch)}}
    @just just::build trixie {{quote(arch)}}
    @just just::build forky {{quote(arch)}}
