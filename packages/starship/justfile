set no-cd

build suite="trixie" arch="all":
    ./scripts/build-targets.ts --build {{quote(suite)}} {{quote(arch)}}
    @just setup-rust {{quote(arch)}}
    ./packages/starship/build.ts {{quote(suite)}} {{quote(arch)}}

build-all arch="all":
    ./scripts/build-targets.ts --build-all {{quote(arch)}}
    @just starship::build bookworm {{quote(arch)}}
    @just starship::build trixie {{quote(arch)}}
    @just starship::build forky {{quote(arch)}}
