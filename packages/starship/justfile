set no-cd

build suite="trixie":
    @just setup-rust
    ./packages/starship/build.ts "{{ suite }}"

build-all:
    @just starship::build bookworm
    @just starship::build trixie
    @just starship::build forky
